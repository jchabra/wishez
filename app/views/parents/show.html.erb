<h1><%= @auth_user.first_name %>'s Profile Page</h1>

<h3>
<%= link_to "Create New Kid", new_kid_path(:parent_id => @auth_user.id) %></h3>

<ul>
  <% @auth_user.kids.each do |kid| %>
    <li class="list"><%= link_to kid.first_name, kid %></li> 
  <% end %>
</ul><!--.kids_list-->
